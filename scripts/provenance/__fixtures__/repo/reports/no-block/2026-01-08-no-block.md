# Fixture report — no provenance block at all

Just prose. This is the common case for every report on disk today
(2026-07-23): the run happened, but no `yaml provenance` block was written.
That's legal and must never be an error.
