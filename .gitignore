node_modules/
dist/
.env
.env.*
!.env.example
.DS_Store
*.tsbuildinfo

# Playwright (e2e/) artifacts — generated by `npm run test:e2e`
test-results/
playwright-report/
.playwright/
blob-report/

# Generated by scripts/provenance/generate.mjs on every predev/prebuild/
# pretest run — never committed on purpose (docs/provenance-model.md §5.2:
# not committing eliminates the drift class entirely instead of policing
# it, and dodges the auto-merge path-guard problem in §5.4).
src/content/provenance.generated.json
